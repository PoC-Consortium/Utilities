Hi. You could add this repo if you like by:
\wget -q -O -  http://package.cryptoguru.org/debian/init.sh

Or you're crazy and do some Burst mining. So you may would like to use creepminer:

\wget -q -O -  http://package.cryptoguru.org/debian/init.sh | bash && apt-get install creepminer
